# Git LFS configuration
.github-db/** filter=lfs diff=lfs merge=lfs -text
*.blob filter=lfs diff=lfs merge=lfs -text

# Ensure JSON files are handled as text
*.json text eol=lf

# Certificate files should never be committed
*.cert binary
*.key binary
